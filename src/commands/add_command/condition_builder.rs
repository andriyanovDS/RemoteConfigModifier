use crate::commands::add_command::conditions::DeviceOSCondition;

#[derive(Debug)]
pub struct ConditionBuilder {
    inner: Parts,
}

#[derive(Debug)]
struct Parts {

}

impl ConditionBuilder {
    fn new() {}
}
